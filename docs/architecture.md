# Architecture

Phase 1 defines contracts only. Runtime modes will be assembled in later phases by dependency
injection around the shared strategy, portfolio, risk, execution, and broker interfaces.

